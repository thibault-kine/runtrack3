function tri(numbers, order) {
    var tri = [];

    if(order == "asc") {
        for(i = 0; i <= numbers.length; i++) {
            if(numbers[i] < numbers[i+1]) {
                tri[i]
            }
        }
    }
    else if(order == "desc") {

    }
}